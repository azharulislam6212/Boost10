{% comment %}
  The footer, composed rather than configured.
{% endcomment %}

{%- liquid
  assign st = section.settings

  assign background_media = st.background_media | default: 'none'
  assign has_background_media = false
  if background_media != 'none'
    assign has_background_media = true
  endif

  assign background_paint = ''
  if has_background_media == false
    assign background_paint = st.background_color | default: ''
  endif

  assign border_line = st.border_style | default: 'none'

  assign bw_top = 0
  assign bw_right = 0
  assign bw_bottom = 0
  assign bw_left = 0
  assign border_any = 0

  if border_line != 'none'
    assign bw_top = st.border_width_top | default: 0 | plus: 0
    assign bw_right = st.border_width_right | default: 0 | plus: 0
    assign bw_bottom = st.border_width_bottom | default: 0 | plus: 0
    assign bw_left = st.border_width_left | default: 0 | plus: 0
    assign border_any = bw_top | plus: bw_right | plus: bw_bottom | plus: bw_left

    assign border_percent = st.border_opacity | default: 100 | plus: 0
    if st.border_color == blank
      assign border_paint = 'color-mix(in srgb, currentColor ' | append: border_percent | append: '%, transparent)'
    else
      assign border_alpha = border_percent | divided_by: 100.0
      assign border_paint = st.border_color | color_modify: 'alpha', border_alpha
    endif
  endif

  assign r_tl = st.border_radius_top_left | default: 40 | plus: 0
  assign r_tr = st.border_radius_top_right | default: 40 | plus: 0
  assign r_bl = st.border_radius_bottom_left | default: 0 | plus: 0
  assign r_br = st.border_radius_bottom_right | default: 0 | plus: 0

  assign r_tl_m = st.border_radius_top_left_mobile | default: r_tl | plus: 0
  assign r_tr_m = st.border_radius_top_right_mobile | default: r_tr | plus: 0
  assign r_bl_m = st.border_radius_bottom_left_mobile | default: r_bl | plus: 0
  assign r_br_m = st.border_radius_bottom_right_mobile | default: r_br | plus: 0

  assign columns_id = 'FooterColumns-' | append: section.id

  assign has_policy = false
  if st.policy_menu != blank and st.policy_menu.links.size > 0
    assign has_policy = true
  endif

  capture section_spacing
    render 'spacing', section: st, declarations: true
  endcapture
-%}

{%- capture footer_styles -%}
  --footer-radius: {{ r_tl }}px {{ r_tr }}px {{ r_br }}px {{ r_bl }}px;
  --footer-radius-mobile: {{ r_tl_m }}px {{ r_tr_m }}px {{ r_br_m }}px {{ r_bl_m }}px;
  --footer-margin-block-start: {{ st.margin_top | default: 0 }}px;
  --footer-margin-block-end: {{ st.margin_bottom | default: 0 }}px;
  {%- if border_any > 0 -%}
    --footer-border-width: {{ bw_top }}px {{ bw_right }}px {{ bw_bottom }}px {{ bw_left }}px;
    --footer-border-style: {{ border_line }};
    --footer-border-color: {{ border_paint }};
  {%- endif -%}
  {%- if background_paint != '' -%}
    background: {{ background_paint }};
  {%- endif -%}
  {{- section_spacing | strip -}}
{%- endcapture -%}

<footer
  class="footer color-scheme-container color-{{ st.color_scheme }}{% if has_background_media %} has-background-media{% endif %}"
  role="contentinfo"
  data-section-spacing
  {%- if st.stack_layer != blank and st.stack_layer != 'default' %} data-section-layer="{{ st.stack_layer }}"{% endif %}
  style="{{ footer_styles | strip_newlines | strip }}"
>
  {%- if has_background_media -%}
    {% render 'background-media', settings: st %}
  {%- endif -%}

  <div class="{% render 'container', width: st.container_width %}">
    {%- if section.blocks.size > 0 -%}

      <motion-effect
        class="footer__panel footer__panel--{{ st.panel_style | default: 'card' }}"
        data-easing="outQuint"
        {% render 'motion-attrs', kind: 'text', section: st, children: true %}
      >
        <footer-columns
          id="{{ columns_id }}"
          class="footer__columns"
          {% if has_policy %}data-has-policy{% endif %}
          data-collapse-below="750"
        >
          {% content_for 'blocks' %}

          {%- if has_policy -%}
            <div class="footer__policies" style="--column-order: 2;">
              <ul class="footer__policies-list" role="list">
                {%- for link in st.policy_menu.links -%}
                  <li>
                    <a class="footer__policy-link" href="{{ link.url }}">{{ link.title }}</a>
                  </li>
                {%- endfor -%}
              </ul>
            </div>
          {%- endif -%}
        </footer-columns>

        <script>
          (function () {
            var el = document.getElementById({{ columns_id | json }});
            if (!el || !window.matchMedia('(min-width: 750px)').matches) return;

            var items = el.querySelectorAll('details.footer-column');
            for (var i = 0; i < items.length; i++) {
              items[i].open = true;
              var summary = items[i].querySelector('summary');
              if (!summary) continue;
              // Not a control at this width, so not a tab stop either. The
              // component sets the same two attributes when it upgrades.
              summary.setAttribute('data-static', '');
              summary.setAttribute('tabindex', '-1');
            }

            el.setAttribute('data-static', '');
          })();
        </script>
      </motion-effect>
    {%- endif -%}
  </div>
</footer>

{% schema %}
{
  "name": "t:sections.footer.name",
  "blocks": [
    {
      "type": "_footer-column"
    },
    {
      "type": "_footer-contact"
    },
    {
      "type": "_footer-utilities"
    },
    {
      "type": "_footer-legal"
    },
    {
      "type": "group"
    },
    {
      "type": "menu"
    },
    {
      "type": "email-signup"
    },
    {
      "type": "social-follow"
    },
    {
      "type": "text"
    },
    {
      "type": "image"
    },
    {
      "type": "button"
    },
    {
      "type": "buttons"
    },
    {
      "type": "divider"
    },
    {
      "type": "spacer"
    },
    {
      "type": "icon-with-text"
    },
    {
      "type": "custom-html"
    },
    {
      "type": "custom-liquid"
    },
    {
      "type": "@app"
    }
  ],
  "settings": [
    {
      "type": "select",
      "id": "container_width",
      "label": "t:shared.container.width.label",
      "info": "t:shared.container.width.info",
      "options": [
        {
          "value": "edge",
          "label": "t:shared.container.width.options__1.label"
        },
        {
          "value": "wide",
          "label": "t:shared.container.width.options__2.label"
        },
        {
          "value": "standard",
          "label": "t:shared.container.width.options__3.label"
        },
        {
          "value": "compact",
          "label": "t:shared.container.width.options__4.label"
        },
        {
          "value": "custom",
          "label": "t:shared.container.width.options__5.label"
        }
      ],
      "default": "standard"
    },
    {
      "type": "select",
      "id": "panel_style",
      "label": "t:sections.footer.settings.panel_style.label",
      "info": "t:sections.footer.settings.panel_style.info",
      "options": [
        {
          "value": "card",
          "label": "t:sections.footer.settings.panel_style.options__1.label"
        },
        {
          "value": "plain",
          "label": "t:sections.footer.settings.panel_style.options__2.label"
        }
      ],
      "default": "card"
    },
    {
      "type": "header",
      "content": "t:sections.footer.headers.legal_links"
    },
    {
      "type": "link_list",
      "id": "policy_menu",
      "label": "t:sections.footer.settings.policy_menu.label"
    },
    {
      "type": "header",
      "content": "t:shared.appearance.header"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:shared.color_scheme.label",
      "default": "scheme-2"
    },
    {
      "type": "select",
      "id": "background_media",
      "label": "t:shared.background.media.label",
      "options": [
        {
          "value": "none",
          "label": "t:shared.background.media.options__1.label"
        },
        {
          "value": "image",
          "label": "t:shared.background.media.options__2.label"
        }
      ],
      "default": "none"
    },
    {
      "type": "color_background",
      "id": "background_color",
      "label": "t:shared.background.color.label",
      "info": "t:shared.background.color.info",
      "visible_if": "{{ section.settings.background_media == 'none' }}"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:shared.background.image.label",
      "visible_if": "{{ section.settings.background_media == 'image' }}"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "t:shared.background.image_mobile.label",
      "info": "t:shared.background.image_mobile.info",
      "visible_if": "{{ section.settings.background_media == 'image' }}"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "t:shared.background.image_position.label",
      "info": "t:shared.background.image_position.info",
      "options": [
        {
          "value": "cover",
          "label": "t:shared.background.image_position.options__1.label"
        },
        {
          "value": "fit",
          "label": "t:shared.background.image_position.options__2.label"
        }
      ],
      "default": "cover",
      "visible_if": "{{ section.settings.background_media == 'image' }}"
    },
    {
      "type": "header",
      "content": "t:blocks.group.headers.borders"
    },
    {
      "type": "select",
      "id": "border_style",
      "label": "t:blocks.group.settings.border_style.label",
      "options": [
        {
          "value": "none",
          "label": "t:shared.options.none"
        },
        {
          "value": "solid",
          "label": "t:shared.options.solid"
        },
        {
          "value": "dashed",
          "label": "t:shared.border.style.options__2.label"
        }
      ],
      "default": "dashed"
    },
    {
      "type": "range",
      "id": "border_width_top",
      "label": "t:shared.border.sides.top.label",
      "info": "t:shared.border.sides.info",
      "min": 0,
      "max": 12,
      "step": 1,
      "unit": "px",
      "default": 1,
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_width_right",
      "label": "t:shared.border.sides.right.label",
      "min": 0,
      "max": 12,
      "step": 1,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_width_bottom",
      "label": "t:shared.border.sides.bottom.label",
      "min": 0,
      "max": 12,
      "step": 1,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_width_left",
      "label": "t:shared.border.sides.left.label",
      "min": 0,
      "max": 12,
      "step": 1,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "range",
      "id": "border_opacity",
      "label": "t:shared.border.opacity.label",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "t:blocks.group.settings.border_color.label",
      "visible_if": "{{ section.settings.border_style != 'none' }}"
    },
    {
      "type": "header",
      "content": "t:shared.corners.header"
    },
    {
      "type": "select",
      "id": "radius_device",
      "label": "t:shared.spacing.devices.label",
      "options": [
        {
          "value": "desktop",
          "label": "t:shared.spacing.devices.options__1.label"
        },
        {
          "value": "mobile",
          "label": "t:shared.spacing.devices.options__2.label"
        }
      ],
      "default": "desktop"
    },
    {
      "type": "range",
      "id": "border_radius_top_left",
      "label": "t:shared.corners.top_left.label",
      "info": "t:shared.corners.paragraph",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 40,
      "visible_if": "{{ section.settings.radius_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "border_radius_top_right",
      "label": "t:shared.corners.top_right.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 40,
      "visible_if": "{{ section.settings.radius_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "border_radius_bottom_left",
      "label": "t:shared.corners.bottom_left.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.radius_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "border_radius_bottom_right",
      "label": "t:shared.corners.bottom_right.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.radius_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "border_radius_top_left_mobile",
      "label": "t:shared.corners.top_left.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.radius_device == 'mobile' }}"
    },
    {
      "type": "range",
      "id": "border_radius_top_right_mobile",
      "label": "t:shared.corners.top_right.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 24,
      "visible_if": "{{ section.settings.radius_device == 'mobile' }}"
    },
    {
      "type": "range",
      "id": "border_radius_bottom_left_mobile",
      "label": "t:shared.corners.bottom_left.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.radius_device == 'mobile' }}"
    },
    {
      "type": "range",
      "id": "border_radius_bottom_right_mobile",
      "label": "t:shared.corners.bottom_right.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ section.settings.radius_device == 'mobile' }}"
    },
    {
      "type": "header",
      "content": "t:shared.animation.header"
    },
    {
      "type": "select",
      "id": "animation_text",
      "label": "t:shared.animation.animation_text.label",
      "options": [
        {
          "value": "inherit",
          "label": "t:shared.animation.animation_text.options__1.label"
        },
        {
          "value": "none",
          "label": "t:shared.animation.animation_text.options__2.label"
        },
        {
          "value": "fade-in",
          "label": "t:shared.animation.animation_text.options__3.label"
        },
        {
          "value": "fade-up",
          "label": "t:shared.animation.animation_text.options__4.label"
        },
        {
          "value": "slide-up",
          "label": "t:shared.animation.animation_text.options__5.label"
        }
      ],
      "default": "inherit"
    },
    {
      "type": "range",
      "id": "animation_duration",
      "label": "t:shared.animation.animation_duration.label",
      "info": "t:shared.animation.animation_duration.info",
      "min": 0,
      "max": 2000,
      "step": 100,
      "unit": "ms",
      "default": 1000
    },
    {
      "type": "range",
      "id": "animation_stagger",
      "label": "t:shared.animation.animation_stagger.label",
      "info": "t:shared.animation.animation_stagger.info",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "ms",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:shared.spacing.section_header"
    },
    {
      "type": "select",
      "id": "padding_device",
      "label": "t:shared.spacing.devices.label",
      "options": [
        {
          "value": "desktop",
          "label": "t:shared.spacing.devices.options__1.label"
        },
        {
          "value": "mobile",
          "label": "t:shared.spacing.devices.options__2.label"
        }
      ],
      "default": "desktop"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:shared.spacing.padding_top.label",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 100,
      "visible_if": "{{ section.settings.padding_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:shared.spacing.padding_bottom.label",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 100,
      "visible_if": "{{ section.settings.padding_device == 'desktop' }}"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "t:shared.spacing.padding_top.label",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 50,
      "visible_if": "{{ section.settings.padding_device == 'mobile' }}"
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "t:shared.spacing.padding_bottom.label",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 50,
      "visible_if": "{{ section.settings.padding_device == 'mobile' }}"
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "t:shared.spacing.margin_top.label",
      "info": "t:shared.spacing.margin_negative.info",
      "min": -160,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "t:shared.spacing.margin_bottom.label",
      "info": "t:shared.spacing.margin_negative.info",
      "min": -160,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "stack_layer",
      "label": "t:shared.spacing.stack_layer.label",
      "info": "t:shared.spacing.stack_layer.info",
      "options": [
        {
          "value": "under",
          "label": "t:shared.spacing.stack_layer.options__1.label"
        },
        {
          "value": "default",
          "label": "t:shared.spacing.stack_layer.options__2.label"
        },
        {
          "value": "over",
          "label": "t:shared.spacing.stack_layer.options__3.label"
        }
      ],
      "default": "default"
    }
  ]
}
{% endschema %}
