{% comment %}
  sections/header.liquid — Boost10
{% endcomment %}

{%- capture header_bay -%}
  {% content_for 'blocks' %}
{%- endcapture -%}

{%- liquid
  assign st = section.settings
  assign menu = st.menu

  # ---- which menu items have a mega menu ----------------------------------
  #
  # Split on the attribute, drop the text before the first one, and take each
  # value up to its closing quote.
  #
  # `escape` on both sides, because the attribute was written escaped and a menu
  # item called "Gut & Digestion" is `Gut &amp; Digestion` in the markup.
  # `downcase` on both sides, because typing a name into a block setting is not
  # the place to demand exact case. The pipes stop "Sale" matching inside
  # "Sale Items".
  assign mega_menu_items = ''
  assign mega_chunks = header_bay | split: 'data-menu-item="'

  for mega_chunk in mega_chunks offset: 1
    assign mega_key = mega_chunk | split: '"' | first | strip | downcase
    if mega_key != blank
      assign mega_menu_items = mega_menu_items | append: '|' | append: mega_key | append: '|'
    endif
  endfor

  # ---- height -------------------------------------------------------------
  case st.header_height
    when 'compact'
      assign row_height = 56
    when 'large'
      assign row_height = 96
    when 'custom'
      assign row_height = st.header_height_custom | default: 72
    else
      assign row_height = 72
  endcase

  # ---- transparency -------------------------------------------------------
  # Read once into three plain variables so the markup below never has to ask
  # which page type it is on again.
  capture transparency
    content_for 'block', type: '_header-transparency', id: 'header-transparency'
  endcapture
  assign transparency = transparency | strip | split: '|'

  assign supports_transparent = false
  if transparency[0] == 'true'
    assign supports_transparent = true
  endif

  assign inverse_logo = false
  if transparency[1] == 'true'
    assign inverse_logo = true
  endif

  assign transparent_text = transparency[2] | default: blank

  # ---- classes ------------------------------------------------------------
  assign header_classes = 'site-header color-' | append: st.color_scheme
  assign header_classes = header_classes | append: ' site-header--shape-' | append: st.header_shape
  if supports_transparent
    assign header_classes = header_classes | append: ' site-header--transparent'
  endif
  if inverse_logo
    assign header_classes = header_classes | append: ' site-header--inverse-logo'
  endif
-%}

{%- liquid
  assign slot_top_start = ''
  assign slot_top_center = ''
  assign slot_top_end = ''
  assign slot_bottom_start = ''
  assign slot_bottom_center = ''
  assign slot_bottom_end = ''
-%}

{%- capture fragment_logo -%}
  {% render 'header-logo', inverse: inverse_logo %}
{%- endcapture -%}

{%- capture fragment_nav -%}
  {% render 'header-nav', menu: menu, mega_menu_items: mega_menu_items %}
{%- endcapture -%}

{%- assign fragment_search = '' -%}

{%- capture fragment_localization -%}
  {% render 'header-localization', context: 'header' %}
{%- endcapture -%}

{%- liquid
  # Logo
  case st.logo_position
    when 'center'
      assign slot_top_center = slot_top_center | append: fragment_logo
    when 'right'
      assign slot_top_end = slot_top_end | append: fragment_logo
    else
      assign slot_top_start = slot_top_start | append: fragment_logo
  endcase

  # Navigation
  #
  # A centred logo and a centred menu both land in the same slot, and the slot
  # centres the pair — so the logo is never on the centre line and the setting
  # looks broken. There is only one centre, so the logo takes it and the menu
  # steps aside to the start. The merchant asked for a centred logo; that is the
  # setting that wins.
  assign menu_place = st.menu_position
  if st.logo_position == 'center' and st.menu_position == 'center' and st.menu_row == 'top'
    assign menu_place = 'left'
  endif

  if st.menu_row == 'bottom'
    case menu_place
      when 'center'
        assign slot_bottom_center = slot_bottom_center | append: fragment_nav
      when 'right'
        assign slot_bottom_end = slot_bottom_end | append: fragment_nav
      else
        assign slot_bottom_start = slot_bottom_start | append: fragment_nav
    endcase
  else
    case menu_place
      when 'center'
        assign slot_top_center = slot_top_center | append: fragment_nav
      when 'right'
        assign slot_top_end = slot_top_end | append: fragment_nav
      else
        assign slot_top_start = slot_top_start | append: fragment_nav
    endcase
  endif

  # Localization
  if st.localization_row == 'bottom'
    if st.localization_position == 'left'
      assign slot_bottom_start = slot_bottom_start | append: fragment_localization
    else
      assign slot_bottom_end = slot_bottom_end | append: fragment_localization
    endif
  else
    if st.localization_position == 'left'
      assign slot_top_start = slot_top_start | append: fragment_localization
    else
      assign slot_top_end = slot_top_end | append: fragment_localization
    endif
  endif

  # Search
  if st.search_row == 'bottom'
    if st.search_position == 'left'
      assign slot_bottom_start = slot_bottom_start | append: fragment_search
    else
      assign slot_bottom_end = slot_bottom_end | append: fragment_search
    endif
  else
    if st.search_position == 'left'
      assign slot_top_start = slot_top_start | append: fragment_search
    else
      assign slot_top_end = slot_top_end | append: fragment_search
    endif
  endif

  assign bottom_row = slot_bottom_start | append: slot_bottom_center | append: slot_bottom_end | strip
-%}

{%- liquid
  assign border_total = st.border_thickness | default: 0 | plus: 0 | times: 2

  # ---- desktop ------------------------------------------------------------
  assign header_block_size = row_height | plus: st.padding_top | plus: st.padding_bottom | plus: border_total

  # ---- mobile: the sliders fall back to the desktop value at 0 -------------
  assign pad_top_mobile = st.padding_top_mobile | default: 0 | plus: 0
  if pad_top_mobile == 0
    assign pad_top_mobile = st.padding_top | default: 0 | plus: 0
  endif

  assign pad_bottom_mobile = st.padding_bottom_mobile | default: 0 | plus: 0
  if pad_bottom_mobile == 0
    assign pad_bottom_mobile = st.padding_bottom | default: 0 | plus: 0
  endif

  assign header_block_size_mobile = 54 | plus: pad_top_mobile | plus: pad_bottom_mobile | plus: border_total

  # ---- the second row, when there is one ----------------------------------
  # `--header-row-height * 0.7` plus its 1px top rule, mirroring header.css.
  if bottom_row != blank
    assign bottom_extra = row_height | times: 70 | divided_by: 100 | plus: 1
    assign header_block_size = header_block_size | plus: bottom_extra

    assign bottom_extra_mobile = 54 | times: 72 | divided_by: 100 | plus: 1
    assign header_block_size_mobile = header_block_size_mobile | plus: bottom_extra_mobile
  endif
-%}

{% style %}
  :root {
    --header-block-size: {{ header_block_size }}px;

    /* The same number, under the name the rest of the theme reads.
       `--header-height` positions the sticky bundle tray, the store locator
       map, the sticky product bar and the search modal.

       It used to be published by `<sticky-header>` alone, from `offsetHeight`,
       so until that module arrived every one of those offsets resolved through
       `var(--header-height, 0px)` to zero and then snapped to the real value on
       upgrade. Stating it here means they start correct.

       The component still writes its own measured value as an inline style on
       `<html>`, which outranks this rule — so a header whose painted height
       differs from the arithmetic (a wrapped menu, a webfont with a taller cap
       height) is still corrected. It simply has nothing left to correct in the
       ordinary case. */
    --header-height: {{ header_block_size }}px;
  }

  @media screen and (max-width: 989px) {
    :root {
      --header-block-size: {{ header_block_size_mobile }}px;
      --header-height: {{ header_block_size_mobile }}px;
    }
  }
{% endstyle %}

{%- capture header_spacing -%}
  {%- render 'spacing', section: section.settings, declarations: true -%}
{%- endcapture -%}
{%- assign header_spacing = header_spacing | strip -%}

<sticky-header
  class="{{ header_classes }}"
  data-sticky-mode="{{ st.sticky_mode }}"
  data-logo-mobile="{{ st.logo_position_mobile | default: 'left' }}"

  data-logo-position="{{ st.logo_position }}"
  data-menu-position="{{ st.menu_position }}"
  data-search-position="{{ st.search_position }}"
  data-threshold="80"
  {% if supports_transparent %}data-transparent{% endif %}
  {% if st.sticky_color_scheme != blank %}data-sticky-scheme="color-{{ st.sticky_color_scheme }}"{% endif %}
  data-section-spacing
  style="
    --header-row-height: {{ row_height }}px;
    --header-border-width: {{ st.border_thickness }}px;
    --header-utility-size: {{ st.utility_size }}px;
    --header-localization-size: {{ st.localization_size }}px;
    {%- if transparent_text != blank %}--header-transparent-text: {{ transparent_text }};{% endif -%}
    {{- header_spacing -}}
  "
>
  <div class="{% render 'container', width: st.container_width, classes: 'site-header__inner' %}">
    <div class="site-header__shell">
      <div class="site-header__row site-header__row--top">
        <div class="site-header__slot site-header__slot--start">

          <button
            type="button"
            class="site-header__burger"
            data-overlay-open="MenuDrawer"
            aria-expanded="false"
            aria-controls="MenuDrawer"
            aria-label="{{ 'accessibility.open_menu' | t }}"
          >
            <span class="site-header__burger-bars" aria-hidden="true"><i></i><i></i></span>
          </button>

          {{ slot_top_start }}
        </div>

        <div class="site-header__slot site-header__slot--center">{{ slot_top_center }}</div>

        <div class="site-header__slot site-header__slot--end">
          {{ slot_top_end }}
          {% content_for 'block', type: '_header-actions', id: 'header-actions' %}
        </div>
      </div>

      {%- if bottom_row != blank -%}
        <div class="site-header__row site-header__row--bottom">
          <div class="site-header__slot site-header__slot--start">{{ slot_bottom_start }}</div>
          <div class="site-header__slot site-header__slot--center">{{ slot_bottom_center }}</div>
          <div class="site-header__slot site-header__slot--end">{{ slot_bottom_end }}</div>
        </div>
      {%- endif -%}
    </div>
  </div>

  <div class="site-header__bay" data-nav-bay hidden>
    {{ header_bay }}
  </div>
</sticky-header>

{% content_for 'block', type: '_header-drawer', id: 'header-drawer' %}

{% schema %}
{
  "name": "t:sections.header.name",
  "tag": "header",
  "class": "header-section",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:sections.header.settings.menu.label",
      "default": "main-menu"
    },
    {
      "type": "select",
      "id": "menu_trigger",
      "label": "t:sections.header.settings.menu_trigger.label",
      "options": [
        {
          "value": "hover",
          "label": "t:sections.header.settings.menu_trigger.options__1.label"
        },
        {
          "value": "click",
          "label": "t:sections.header.settings.menu_trigger.options__2.label"
        }
      ],
      "default": "hover"
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.logo"
    },
    {
      "type": "select",
      "id": "logo_position",
      "label": "t:shared.position.label",
      "options": [
        {
          "value": "left",
          "label": "t:shared.position.options__1.label"
        },
        {
          "value": "center",
          "label": "t:shared.position.options__2.label"
        },
        {
          "value": "right",
          "label": "t:shared.position.options__3.label"
        }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "logo_position_mobile",
      "label": "t:sections.header.settings.logo_position_mobile.label",
      "info": "t:sections.header.settings.logo_position_mobile.info",
      "options": [
        {
          "value": "left",
          "label": "t:shared.position.options__1.label"
        },
        {
          "value": "center",
          "label": "t:shared.position.options__2.label"
        }
      ],
      "default": "left"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "t:sections.header.settings.logo.label"
    },
    {
      "type": "image_picker",
      "id": "logo_inverse",
      "label": "t:sections.header.settings.logo_inverse.label",
      "info": "t:sections.header.settings.logo_inverse.info"
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "t:sections.header.settings.logo_width.label",
      "min": 60,
      "max": 320,
      "step": 4,
      "unit": "px",
      "default": 140
    },
    {
      "type": "range",
      "id": "logo_width_mobile",
      "label": "t:sections.header.settings.logo_width_mobile.label",
      "min": 60,
      "max": 240,
      "step": 5,
      "unit": "px",
      "default": 110
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.menu"
    },
    {
      "type": "select",
      "id": "menu_position",
      "label": "t:shared.position.label",
      "options": [
        {
          "value": "left",
          "label": "t:shared.position.options__1.label"
        },
        {
          "value": "center",
          "label": "t:shared.position.options__2.label"
        },
        {
          "value": "right",
          "label": "t:shared.position.options__3.label"
        }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "menu_row",
      "label": "t:shared.row.label",
      "options": [
        {
          "value": "top",
          "label": "t:shared.row.options__1.label"
        },
        {
          "value": "bottom",
          "label": "t:shared.row.options__2.label"
        }
      ],
      "default": "top"
    },
    {
      "type": "range",
      "id": "menu_size",
      "label": "t:shared.typography.size.label",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "select",
      "id": "menu_font",
      "label": "t:shared.typography.font.label",
      "options": [
        {
          "value": "body",
          "label": "t:shared.typography.font.options__1.label"
        },
        {
          "value": "heading",
          "label": "t:shared.typography.font.options__2.label"
        },
        {
          "value": "accent",
          "label": "t:shared.typography.font.options__3.label"
        }
      ],
      "default": "body"
    },
    {
      "type": "select",
      "id": "menu_case",
      "label": "t:shared.typography.case.label",
      "options": [
        {
          "value": "none",
          "label": "t:shared.typography.case.options__1.label"
        },
        {
          "value": "uppercase",
          "label": "t:shared.typography.case.options__2.label"
        }
      ],
      "default": "none"
    },
    {
      "type": "select",
      "id": "menu_marker",
      "label": "t:sections.header.settings.menu_marker.label",
      "info": "t:sections.header.settings.menu_marker.info",
      "options": [
        {
          "value": "pill",
          "label": "t:sections.header.settings.menu_marker.options__1.label"
        },
        {
          "value": "underline",
          "label": "t:sections.header.settings.menu_marker.options__2.label"
        },
        {
          "value": "none",
          "label": "t:sections.header.settings.menu_marker.options__3.label"
        }
      ],
      "default": "pill"
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.search"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "t:sections.header.settings.show_search.label",
      "default": true
    },
    {
      "type": "select",
      "id": "search_position",
      "label": "t:shared.position.label",
      "options": [
        {
          "value": "left",
          "label": "t:shared.position.options__1.label"
        },
        {
          "value": "right",
          "label": "t:shared.position.options__3.label"
        }
      ],
      "default": "right"
    },
    {
      "type": "select",
      "id": "search_row",
      "label": "t:shared.row.label",
      "options": [
        {
          "value": "top",
          "label": "t:shared.row.options__1.label"
        },
        {
          "value": "bottom",
          "label": "t:shared.row.options__2.label"
        }
      ],
      "default": "top"
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.account"
    },
    {
      "type": "paragraph",
      "content": "t:sections.header.paragraphs.account"
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "t:sections.header.settings.show_account.label",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.localization"
    },
    {
      "type": "checkbox",
      "id": "show_country",
      "label": "t:sections.header.settings.show_country.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_flag",
      "label": "t:sections.header.settings.show_flag.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_language",
      "label": "t:sections.header.settings.show_language.label",
      "default": true
    },
    {
      "type": "select",
      "id": "localization_search",
      "label": "t:sections.header.settings.localization_search.label",
      "info": "t:sections.header.settings.localization_search.info",
      "options": [
        {
          "value": "auto",
          "label": "t:sections.header.settings.localization_search.options__1.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.localization_search.options__2.label"
        },
        {
          "value": "never",
          "label": "t:sections.header.settings.localization_search.options__3.label"
        }
      ],
      "default": "always"
    },
    {
      "type": "select",
      "id": "localization_font",
      "label": "t:shared.typography.font.label",
      "options": [
        {
          "value": "body",
          "label": "t:shared.typography.font.options__1.label"
        },
        {
          "value": "heading",
          "label": "t:shared.typography.font.options__2.label"
        },
        {
          "value": "accent",
          "label": "t:shared.typography.font.options__3.label"
        }
      ],
      "default": "heading"
    },
    {
      "type": "range",
      "id": "localization_size",
      "label": "t:shared.typography.size.label",
      "min": 11,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "select",
      "id": "localization_position",
      "label": "t:shared.position.label",
      "options": [
        {
          "value": "left",
          "label": "t:shared.position.options__1.label"
        },
        {
          "value": "right",
          "label": "t:shared.position.options__3.label"
        }
      ],
      "default": "right"
    },
    {
      "type": "select",
      "id": "localization_row",
      "label": "t:shared.row.label",
      "options": [
        {
          "value": "top",
          "label": "t:shared.row.options__1.label"
        },
        {
          "value": "bottom",
          "label": "t:shared.row.options__2.label"
        }
      ],
      "default": "top"
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.appearance"
    },
    {
      "type": "select",
      "id": "container_width",
      "label": "t:shared.container.width.label",
      "info": "t:shared.container.width.info",
      "options": [
        {
          "value": "edge",
          "label": "t:shared.container.width.options__1.label"
        },
        {
          "value": "wide",
          "label": "t:shared.container.width.options__2.label"
        },
        {
          "value": "standard",
          "label": "t:shared.container.width.options__3.label"
        },
        {
          "value": "compact",
          "label": "t:shared.container.width.options__4.label"
        },
        {
          "value": "custom",
          "label": "t:shared.container.width.options__5.label"
        }
      ],
      "default": "wide"
    },
    {
      "type": "select",
      "id": "header_shape",
      "label": "t:sections.header.settings.header_shape.label",
      "info": "t:sections.header.settings.header_shape.info",
      "options": [
        {
          "value": "bar",
          "label": "t:sections.header.settings.header_shape.options__1.label"
        },
        {
          "value": "pill",
          "label": "t:sections.header.settings.header_shape.options__2.label"
        }
      ],
      "default": "pill"
    },
    {
      "type": "select",
      "id": "header_height",
      "label": "t:sections.header.settings.header_height.label",
      "options": [
        {
          "value": "compact",
          "label": "t:sections.header.settings.header_height.options__1.label"
        },
        {
          "value": "standard",
          "label": "t:sections.header.settings.header_height.options__2.label"
        },
        {
          "value": "large",
          "label": "t:sections.header.settings.header_height.options__3.label"
        },
        {
          "value": "custom",
          "label": "t:sections.header.settings.header_height.options__4.label"
        }
      ],
      "default": "standard"
    },
    {
      "type": "range",
      "id": "header_height_custom",
      "label": "t:sections.header.settings.header_height_custom.label",
      "info": "t:sections.header.settings.header_height_custom.info",
      "min": 48,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 72
    },
    {
      "type": "select",
      "id": "sticky_mode",
      "label": "t:sections.header.settings.sticky_mode.label",
      "options": [
        {
          "value": "never",
          "label": "t:sections.header.settings.sticky_mode.options__1.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_mode.options__2.label"
        },
        {
          "value": "scroll-up",
          "label": "t:sections.header.settings.sticky_mode.options__3.label"
        },
        {
          "value": "scroll-down",
          "label": "t:sections.header.settings.sticky_mode.options__4.label"
        }
      ],
      "default": "always"
    },
    {
      "type": "range",
      "id": "border_thickness",
      "label": "t:sections.header.settings.border_thickness.label",
      "min": 0,
      "max": 6,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.header.headers.utilities"
    },
    {
      "type": "range",
      "id": "utility_size",
      "label": "t:shared.typography.size.label",
      "min": 11,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "header",
      "content": "t:shared.color_scheme.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.header.settings.color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "sticky_color_scheme",
      "label": "t:sections.header.settings.sticky_color_scheme.label",
      "info": "t:sections.header.settings.sticky_color_scheme.info",
      "default": "scheme-3"
    },
    {
      "type": "paragraph",
      "content": "t:sections.header.paragraphs.transparent"
    },
    {
      "type": "header",
      "content": "t:shared.spacing.section_header"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:shared.spacing.padding_top.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:shared.spacing.padding_bottom.label",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "t:shared.spacing.padding_top_mobile.label",
      "info": "t:shared.spacing.padding_top_mobile.info",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "t:shared.spacing.padding_bottom_mobile.label",
      "info": "t:shared.spacing.padding_bottom_mobile.info",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "default": 0
    }
  ],
  "blocks": [
    {
      "type": "mega-menu"
    },
    {
      "type": "mega-menu-simple"
    },
    {
      "type": "@app"
    }
  ],
  "enabled_on": {
    "groups": [
      "header"
    ]
  }
}
{% endschema %}
