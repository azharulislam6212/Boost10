{% doc %}
  blocks/_product-selling-plans.liquid — private.
{% enddoc %}

{%- liquid
  assign product = closest.product
-%}

{%- if product -%}
  <div {{ block.shopify_attributes }}>
    {% render 'selling-plans', product: product %}
  </div>
{%- endif -%}

{% schema %}
{
  "name": "t:blocks.product_selling_plans.name",
  "tag": null,
  "settings": []
}
{% endschema %}
