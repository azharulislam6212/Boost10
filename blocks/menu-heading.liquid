{% comment %}
  blocks/menu-heading.liquid — Boost10

  A label that groups the blocks under it, with an optional link.

  Rendered as a `<p>`, never as `<h3>`. The panel is inside `<nav>`, and a
  heading there joins the page's heading outline: a screen reader user tabbing
  through headings would land on "New in" between the page title and the first
  real section, with no way to tell it came from a menu.

  The visual weight is CSS. The semantics stay out of the outline.
{% endcomment %}

{%- liquid
  assign st = block.settings
-%}

<div
  class="megamenu__item megamenu__item--heading"
  style="--span: {{ st.column_span }};"
  {{ block.shopify_attributes }}
>
  {%- if st.link != blank -%}
    <a class="megamenu__eyebrow megamenu__eyebrow--link" href="{{ st.link }}">
      <span>{{ st.text | escape }}</span>
      {% render 'icon', icon: 'arrow-right', size: 14 %}
    </a>
  {%- else -%}
    <p class="megamenu__eyebrow">{{ st.text | escape }}</p>
  {%- endif -%}
</div>

{% schema %}
{
  "name": "t:blocks.menu_heading.name",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "t:shared.text.label",
      "default": "Featured"
    },
    {
      "type": "url",
      "id": "link",
      "label": "t:shared.link.label"
    },
    {
      "type": "range",
      "id": "column_span",
      "label": "t:blocks.shared.column_span.label",
      "info": "t:blocks.shared.column_span.info",
      "min": 1,
      "max": 12,
      "step": 1,
      "default": 12
    }
  ]
}
{% endschema %}
