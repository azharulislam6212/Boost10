 
    {{ 'base.css' | asset_url | stylesheet_tag: preload: true }}
    {%- comment -%}
      Loaded after base.css and preloaded with it. The header is above the fold
      on every page in the store, so it is never a candidate for a deferred
      stylesheet: arriving late means the navigation paints unstyled.
    {%- endcomment -%}
    {{ 'header.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'product-card.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'rtl-support.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'animations.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'swiper.css' | asset_url | stylesheet_tag: preload: true }}

   